# Prompt Manager 页面配置操作手册

> 面向**运营 / 业务方**的逐页操作指南。开发相关的架构、API、SDK 说明请看 `README.md`。

- 主管理台：`http://<服务器IP>:8900`
- Agent 台（流程树解析 / LLM 对话）：`http://<服务器IP>:8901`
- 默认账号：`admin` / `admin123456`（首次登录后请立即修改密码）

---

## 目录

1. [登录与页面总览](#1-登录与页面总览)
2. [提示词管理](#2-提示词管理)
3. [机器人配置看板](#3-机器人配置看板)
4. [知识库管理](#4-知识库管理)
5. [流程树管理](#5-流程树管理)
6. [账户权限管理](#6-账户权限管理)
7. [LLM 配置](#7-llm-配置)
8. [常见操作场景速查](#8-常见操作场景速查)
9. [常见问题排查](#9-常见问题排查)

---

## 1. 登录与页面总览

### 1.1 登录

1. 浏览器打开 `http://<服务器IP>:8900`
2. 输入用户名 / 密码 → 点击「登录」

> **注意**：服务每次重启会清空所有登录会话，需要重新登录。这是有意设计（避免长期无主 token）。

### 1.2 顶部信息栏

| 位置       | 内容                                                                 |
| ---------- | -------------------------------------------------------------------- |
| 左侧       | 系统标题                                                             |
| 右侧       | 用户名 · 权限等级（`高级权限` / `普通权限`）                     |
| 权限提示   | 普通用户会额外显示 `管理 N 科`；未配置管理科室时显示 `⚠ 未配置管理科室` |
| 图标按钮   | 👤 账户权限（仅 admin）· 🔒 修改密码 · ⚙️ LLM 配置 · 🚪 退出登录        |

### 1.3 模式切换（按权限动态显示）

顶部四个切换按钮：

| 按钮           | 需要的权限            | 用途                       |
| -------------- | --------------------- | -------------------------- |
| **提示词管理** | `can_prompt_view`   | 管理系统提示词 / 评分提示词 |
| **知识库管理** | `can_knowledge_view`| 管理答疑 / 问诊 / 约束等知识 |
| **流程树管理** | `can_flow_view`     | 上传流程图并解析为自然语言  |
| **账户权限**   | admin 专属            | 创建账号、分配权限与管理科室 |

### 1.4 筛选栏

| 控件         | 说明                                                                             |
| ------------ | -------------------------------------------------------------------------------- |
| **科室**     | `全部科室（全部 N 科 / 权限 N 科）` + 具体科室；普通用户只看得到自己被授权的科室 |
| **平台**     | 全部平台 / 小红书 / 百度 / 抖音 / 快手 / 微信 / 通用                             |
| **场景**     | 仅提示词页显示：系统提示词 / 暖场语 / 知识模板 / 动作描述 / **评分** / 通用       |
| **搜索框**   | 提示词页搜名称与描述；知识库页搜**知识内容**（全局）；流程树页搜**流程树记录**    |

> **"全部科室"含义**：admin = 所有科室；普通用户 = 自己 `管理科室` 的并集。选中后左侧列表会展示权限范围内所有科室的数据。

### 1.5 自定义科室 / 平台 / 知识类型

科室、平台、知识类型三个下拉框的**最后一项**是配置入口，点击后弹出维护窗口：

| 下拉框                       | 入口项              | 维护内容                    |
| ---------------------------- | ------------------- | --------------------------- |
| 科室（所有页面）             | `⚙ 自定义科室...` | 科室中文名 + 英文标记       |
| 平台（所有页面）             | `⚙ 自定义平台...` | 平台中文名 + 英文标记       |
| 类型（仅知识库管理页）       | `⚙ 自定义类型...` | 知识记录类型名称            |

> 需要 **提示词管理 - 编辑** 权限才会显示该入口项；删除需要 **提示词管理 - 删除** 权限。

#### 弹窗操作

| 操作     | 步骤                                                                 |
| -------- | -------------------------------------------------------------------- |
| **新增** | 在底部输入「中文名称」+「英文标记」（知识类型只需名称）→ 点 `+ 新增` |
| **修改** | 点某行 `编辑` → 修改名称 / 标记 → 点 `保存`                      |
| **删除** | 点某行 `删除` → 二次确认                                           |
| **完成** | 点右下角 `完成` 关闭弹窗                                           |

保存后**立即生效**，下拉框选项与当前列表会自动刷新，无需刷新页面或重启服务。

#### 注意事项

| 规则                     | 说明                                                                          |
| ------------------------ | ----------------------------------------------------------------------------- |
| **英文标记格式**         | 只能用字母、数字、下划线、连字符（如 `ent`、`kuaishou_live`）              |
| **标记唯一**             | 同类型下英文标记不能重复                                                      |
| **修改标记会级联更新**   | 会同步改写提示词 / 知识库 / 流程树 / 机器人配置中的旧标记，**不会丢数据**；操作前有二次确认 |
| **被占用时无法删除**     | 若仍有数据在用，会提示 `仍被 N 条数据引用，无法删除`；需先把这些数据改到其他选项 |
| **知识类型只有中文名**   | 不需要英文标记，名称本身即标识                                                |

> **建议**：新增科室/平台后，先去「账户权限」把该科室分配给对应运营账号，否则普通用户看不到。

---

## 2. 提示词管理

### 2.1 左侧列表

每个卡片展示：**名称** · 科室标签 · 平台标签 · 场景标签 · 版本号（`v12`）· 启用状态 · 描述摘要。点击卡片查看详情。

右上角 `+ 新建提示词`（需 `can_prompt_edit`）。

### 2.2 详情页布局（从上到下）

```
① 元信息    科室 · 平台 · 场景 · 版本 · 状态 · 更新时间
② 描述 / 标签
③ 机器人配置看板   ← 只在"当前科室+平台有机器人配置"时出现（详见第 3 章）
④ 提示词内容
⑤ 变量标记映射
⑥ 版本历史（点「版本历史」按钮展开）
```

右上角操作按钮：

| 按钮                    | 说明                                                     |
| ----------------------- | -------------------------------------------------------- |
| `编辑`                | 进入编辑态修改内容 / 变量映射 / 机器人配置                |
| `版本历史`            | 展开历史版本列表，可查看内容、回滚、删除历史版本           |
| `停用` / `启用`     | **按当前状态自动切换**：启用中显示红色`停用`，停用中显示绿色`启用`（详见 2.7） |

### 2.3 新建提示词

| 字段         | 必填 | 说明                                                                    |
| ------------ | ---- | ----------------------------------------------------------------------- |
| **科室**     | ✅   | 普通用户只能选自己管理的科室，否则保存报 `403`                          |
| **平台**     | ✅   | 小红书 / 百度 / 抖音 / 快手 / 微信 / 通用                               |
| **场景**     | ✅   | 系统提示词 / 暖场语 / 知识模板 / 动作描述 / 评分 / 通用                 |
| **描述**     | ❌   | 简短说明，会显示在左侧列表                                              |
| **名称**     | ✅   | 全局唯一。**强烈建议**按 `{科室}_{平台}_{场景}` 命名，如 `hair_xhs_system` |
| **提示词内容** | ✅ | 模板正文，可写内置变量与自定义占位符                                    |
| **变量标记映射** | ❌ | 把外部字段绑定到自定义占位符（见 2.5）                                 |
| **变更说明** | ❌   | 本次修改的原因，记入版本历史                                            |

> **命名很重要**：qwen 服务按 `(科室, 平台, 场景)` → 提示词名 的映射查找模板。名称不规范会导致业务侧取不到、走兜底提示词。

### 2.4 内置变量（直接写在内容里即可）

| 变量           | 替换为                              |
| -------------- | ----------------------------------- |
| `{公司}`     | 雍禾 / 牙博士 / 邦泰 …（由机器人配置的公司名决定） |
| `{域中文}`   | 植发科 / 口腔科 / 皮肤科 …          |
| `{域英文}`   | hair / dentistry / dermatology …    |
| `{时间}`     | 当前时间                            |
| `{轮次}`     | `第3轮`                           |
| `{轮次k}`    | `3`                               |
| `{套联描述}` | 业务侧传入的套联话术                |
| `{动作描述}` | 本轮回复意图，如 `\n回复意图：问诊、套联` |
| `{知识描述}` | 检索到的知识库内容                  |
| `{暖场描述}` | 暖场语                              |

### 2.5 变量标记映射

用于把**外部动态字段**注入到你自定义的占位符。

**操作**：编辑态点 `+ 添加映射` → 左侧填「变量名」，右侧填「占位符标记」→ 保存。

**示例**：

| 变量名（外部字段）  | 占位符标记（内容里写这个） |
| ------------------- | -------------------------- |
| `contact_sample`  | `{套联示例}`             |
| `answer_sample`   | `{答疑示例}`             |
| `answer_ban`      | `{答疑约束}`             |
| `flow_logic`      | `{流程逻辑}`             |
| `disable_word`    | `{违禁规则}`             |

然后在提示词内容里写：

```
## 参考话术
{套联示例}

## 必须遵守
{答疑约束}
{违禁规则}
```

业务侧调用时会把对应内容填进去。

### 2.6 版本管理

| 操作           | 说明                                                        |
| -------------- | ----------------------------------------------------------- |
| 保存修改       | 内容有变化时版本号自动 +1，旧版本进入历史                    |
| 查看版本历史   | 点「版本历史」→ 展开列表 → 点某个版本查看内容                |
| 回滚           | 在版本历史里点「回滚」→ 该版本内容成为新的当前版本（版本号继续 +1） |
| 删除历史版本   | 只能删非当前版本                                            |

> **发布建议**：改完内容后一定填「变更说明」，出问题时便于快速定位并回滚。

### 2.7 启用 / 停用切换

详情页右上角的按钮会**按当前状态自动变化**，点击即完成状态转换：

| 当前状态 | 按钮显示         | 点击后            |
| -------- | ---------------- | ----------------- |
| 启用     | 红色 `停用`    | 变为**停用**      |
| 停用     | 绿色 `启用`    | 恢复为**启用**    |

**状态查看位置**：

- 左侧列表卡片上的绿色 `启用` / 灰色 `停用` 标签
- 详情页元信息行的「状态」字段

**影响范围**：

| 状态 | SDK / 业务侧行为                                          |
| ---- | --------------------------------------------------------- |
| 启用 | 可正常取到该提示词                                        |
| 停用 | **取不到**该提示词，业务侧会走兜底逻辑                    |

> **说明**：停用是**软删除**，内容与全部历史版本都完整保留，随时可以一键恢复启用。恢复后缓存立即刷新，业务侧无需重启。

> **权限**：需要 `提示词管理 - 编辑` 权限；普通用户还需该提示词的科室在自己的「管理科室」范围内。

---

## 3. 机器人配置看板

**位置**：提示词管理 → 选中某条提示词 → 详情页「描述」下方、「提示词内容」上方。

### 3.1 显示规则

| 状态       | 当前科室+平台**有**机器人配置 | 当前科室+平台**无**机器人配置              |
| ---------- | ----------------------------- | ------------------------------------------ |
| **查看态** | ✅ 显示看板                    | ❌ 不显示                                   |
| **编辑态** | ✅ 显示看板                    | ✅ 显示看板 + 空态提示 + `+ 新增条目` 按钮 |

> 想给一个**尚无任何机器人配置**的科室+平台新增机器人，先点「编辑」进入编辑态。

### 3.2 工具栏（检索 + 分页）

看板标题「机器人配置（科室 / 平台）」**右侧**依次为：

| 控件                    | 说明                                                            |
| ----------------------- | --------------------------------------------------------------- |
| **搜索框**              | 按「**公司名**」或「**机器人ID**」模糊检索，大小写不敏感         |
| **分页器** `‹ 1/3 ›`  | 每页 **5 条**；左右箭头翻页，到首/末页时对应箭头自动置灰        |
| **共 N 条**             | 当前检索结果的总条数（不是全部条数）                            |
| `+ 新增条目`          | 新增机器人配置                                                  |

**行为说明**：

- 输入检索词后**自动回到第 1 页**
- 检索无结果时表格显示 `没有匹配「xxx」的机器人配置`
- 切换到其他提示词时，检索词与页码**自动重置**
- 检索条件变化导致总页数减少时，页码会自动收敛到最后一页

> **适用场景**：某些科室+平台下机器人数量较多（如皮肤科/百度有 7 个以上），用检索快速定位到目标机器人，避免在长列表里滚动查找。

### 3.3 表格字段

| 列              | 说明                                                        |
| --------------- | ----------------------------------------------------------- |
| **机器人ID**    | 业务侧的 bot_id（唯一）                                     |
| **科室**        | 该机器人归属科室                                            |
| **平台**        | 该机器人归属平台                                            |
| **公司名**      | 雍禾 / 牙博士 / 邦泰 …，会替换提示词里的 `{公司}`         |
| **提示词版本**  | 绿色 `vN` = 实际生效版本；灰色 `无` = 该科室+平台无提示词 |
| **状态**        | 启用 / 停用（停用的机器人不会同步到业务服务）                |
| **更新**        | 最后修改时间                                                |
| **操作**        | `编辑` / `删除`                                         |

### 3.4 新增 / 编辑机器人配置

点 `+ 新增条目` 或某行的 `编辑`，出现表单：

| 字段             | 说明                                                                       |
| ---------------- | -------------------------------------------------------------------------- |
| **机器人ID**     | 新建时必填；编辑时不可改                                                   |
| **科室**         | 默认取当前提示词的科室                                                     |
| **平台**         | 默认取当前提示词的平台                                                     |
| **状态**         | 启用 / 停用                                                                |
| **提示词版本**   | **`-1` = 跟随最新版本**（推荐）；填 `>=0` = 锁定到该固定版本           |
| **公司名**       | 可从下拉候选选，也可直接输入新公司名                                       |

填完点 `保存`。

### 3.5 提示词版本锁定（重要）

**用途**：某个机器人需要停留在旧版提示词（如灰度回滚、A/B 对照）时使用。

#### 两种修改方式

**方式一：看板内直接下拉切换（推荐，最快）**

「提示词版本」列本身就是一个下拉框，直接点开选择即可，**无需进入编辑态**：

| 下拉选项            | 含义                                        |
| ------------------- | ------------------------------------------- |
| `跟随最新 (vN)`   | 始终使用最新版本，括号内是当前实际生效版本   |
| `锁定 v12`        | 固定在 v12，后续提示词升版也不影响该机器人   |

选中后立即保存并刷新，右下角会提示 `xxx 已锁定 v12` 或 `xxx 已改为跟随最新版本`。

> 下拉里的可选版本来自该提示词的**当前版本 + 全部历史版本**（从新到旧排列）。

**方式二：编辑表单里改**

点该行 `编辑` → 表单中「提示词版本」填数字 → `保存`。

| 填写值   | 行为                                                        |
| -------- | ----------------------------------------------------------- |
| `-1`   | 该机器人始终使用该科室+平台的**最新版本**提示词             |
| `5`    | 该机器人**锁定在 v5**；提示词升到 v6/v7 也不影响它          |

#### 显示样式

| 样式                    | 含义                              |
| ----------------------- | --------------------------------- |
| 绿色 `v18`（跟随最新） | 未锁定，自动吃最新版本            |
| 橙色 `v12 锁定`        | 已锁定在 v12                      |
| 灰色 `无`              | 该科室+平台还没有提示词            |

鼠标悬停在版本下拉上会显示完整说明，如：`已锁定 v12，当前生效：v12（若该版本不存在会自动降级为最新）`

#### 降级保护

若锁定的版本已被删除，系统会自动降级为最新版本，不会出现取不到提示词的情况。

#### 生效路径

业务侧调用 SDK 的 `get_resolved_by_bot(bot_id=...)` 时，会先查该机器人配置决定用哪个版本，再取对应提示词。

> **操作建议**：
> 1. 灰度验证新提示词时，先给部分机器人配 `跟随最新`（吃新版），其余 `锁定` 到旧版本号
> 2. 验证通过后统一改回 `跟随最新`
> 3. 不要长期锁定，否则后续提示词优化对该机器人全部失效

### 3.6 热更新说明

在这里改机器人配置**不需要重启业务服务**：

1. 保存后写入数据库
2. 业务侧（qwen 服务）每 30 秒轮询一次
3. 检测到变化后自动重建 `科室/平台/公司名` 映射并重载该机器人的知识库

业务服务日志会打印：`[热更新] 机器人配置已同步: N 条，共 M 条映射`

---

## 4. 知识库管理

### 4.1 结构说明

- 一个「知识库」= 一个 `(科室, 平台)` 组合，**唯一**
- 一个知识库内包含多条「知识条目」，每条有：**内容** + **类型** + **机器人ID**

### 4.2 左侧列表

每个卡片展示：`科室 - 平台` · 科室标签 · 平台标签 · `共 N 条`。

右上角 `+ 新建知识库`（需 `can_knowledge_edit`）。

### 4.3 右侧详情工具栏

| 按钮              | 权限                    | 说明                                   |
| ----------------- | ----------------------- | -------------------------------------- |
| `📤 导出`       | `can_knowledge_view`  | 按当前筛选条件导出 Excel               |
| `📥 导入`       | `can_knowledge_view`  | 上传 Excel 批量导入                    |
| `+ 新增条目`    | `can_knowledge_edit`  | 新增单条知识                           |
| `删除知识库`    | `can_knowledge_delete`| 删除整个知识库（含全部条目）           |

### 4.4 知识类型（默认 11 类，可自定义）

| 类型         | 用途                                       |
| ------------ | ------------------------------------------ |
| **答疑**     | 回答访客常见问题                           |
| **问诊**     | 主动询问病情 / 诉求的话术                  |
| **套联**     | 引导访客留下联系方式                       |
| **流程**     | 业务流程说明                               |
| **默认认知** | 品牌 / 项目基础认知                        |
| **额外**     | 其他补充内容                               |
| **问诊约束** | 问诊时必须遵守的规则                       |
| **答疑约束** | 答疑时必须遵守的规则                       |
| **套联约束** | 套联时必须遵守的规则                       |
| **核心约束** | 全局必须遵守的红线                         |
| **违禁词**   | 绝对禁止出现的词句                         |

> **可自定义**：顶部「类型」下拉框最后一项 `⚙ 自定义类型...` 可新增 / 改名 / 删除知识类型，详见 [1.5 自定义科室 / 平台 / 知识类型](#15-自定义科室--平台--知识类型)。已被知识记录使用的类型无法删除。

### 4.5 机器人ID 字段（重点）

| 填写值        | 含义                                                                 |
| ------------- | -------------------------------------------------------------------- |
| **留空（通用）** | 所有机器人都能命中这条知识                                        |
| 具体 ID（如 `9378`） | 只有该机器人命中；**同时也会命中所有留空的通用记录**       |

**示例**：知识库里有 3 条：

| 内容            | 机器人ID  |
| --------------- | --------- |
| A. 品牌介绍     | 留空      |
| B. 雍禾专属优惠 | `9378`  |
| C. 碧莲盛优惠   | `9125`  |

- 机器人 `9378` 检索 → 命中 **A + B**
- 机器人 `9125` 检索 → 命中 **A + C**
- 不指定机器人 → 命中 **A + B + C**

> **用法建议**：通用话术（品牌介绍、基础答疑、违禁词）留空；机构专属内容（价格、优惠、门店）填具体机器人ID。

### 4.6 新增知识条目

点 `+ 新增条目`：

1. **知识类型** — 从 11 类中选
2. **机器人ID** — 选 `通用（空）` / 已有ID / `自定义...`（手输新ID）
3. **内容** — 知识正文

点确认后立即保存。

### 4.7 编辑 / 删除条目

鼠标悬停在某条记录上 → 出现 `编辑` / `删除` 按钮。

编辑态可同时修改：内容、类型、机器人ID。

### 4.8 筛选与全局搜索

| 控件           | 行为                                                                 |
| -------------- | -------------------------------------------------------------------- |
| **类型筛选**   | 全部类型 + 11 类具体类型                                             |
| **机器人筛选** | `全部机器人` / `通用（空）` / 具体ID                             |
| **搜索框**     | **全局搜索知识内容**，不受科室/平台筛选限制                          |

**全局搜索行为**：

1. 后端按内容模糊匹配，返回所有命中的知识库
2. 前端自动选中第一个命中的知识库
3. 右侧只显示命中关键词的条目

> 选「具体机器人」时会同时显示该机器人 + 通用（空）的记录；选「通用（空）」时只显示空机器人ID的记录。

### 4.9 Excel 导入导出

#### 导出

1. 先设好筛选条件（科室 / 平台 / 关键词）
2. 点 `📤 导出`
3. 下载 `knowledge_YYYYMMDD_HHMMSS.xlsx`

**表头固定为**：`科室` `平台` `类型` `机器人ID` `知识内容`

#### 导入

1. 准备 Excel，**表头必须完全一致**（顺序也要一致）
2. 点 `📥 导入` 选文件
3. 确认提示后上传
4. 结果提示：`导入完成：新建 X，更新 Y，跳过 Z，共 N 条`

**导入规则**：

| 情况                        | 处理                                     |
| --------------------------- | ---------------------------------------- |
| `(科室, 平台)` 已存在     | 追加到现有内容，自动去重                 |
| `(科室, 平台)` 不存在     | 新建知识库                               |
| 科室 / 平台 / 内容为空      | 跳过该行                                 |
| 普通用户导入越权科室的行     | 跳过该行（计入 `skipped`）             |

> **批量维护推荐流程**：导出 → Excel 里批量编辑 → 导入。比在页面里逐条改快得多。

---

## 5. 流程树管理

### 5.1 用途

把「业务流程图」或「客服对话截图」上传后，由 AI 抽象成**一小段自然语言**的核心场景分支逻辑，可直接拼进提示词使用。

### 5.2 层级结构

- 一个「流程树库」= 一个 `(科室, 平台)` 组合，**唯一**
- 一个库下可有多条「解析记录」，每条对应一次上传的图片 / PDF

### 5.3 左侧列表

卡片展示：`科室 - 平台` · 标签 · 记录数。右上角 `+ 新建流程树库`（需 `can_flow_edit`）。

### 5.4 上传并解析

1. 选中一个流程树库
2. 点 `+ 上传解析`
3. 弹窗里：
   - **选择文件** — 支持 PNG / JPG / GIF / WEBP / PDF
   - **机器人ID** — 可选，留空表示通用
4. 点确认 → 后台异步解析

**状态流转**：

| 状态         | 含义                                   |
| ------------ | -------------------------------------- |
| `pending`  | 已上传，排队等待解析                   |
| `parsing`  | 正在解析（页面会自动轮询刷新）         |
| `success`  | 解析成功，已生成自然语言描述           |
| `partial`  | 多页 PDF 部分页成功                    |
| `failed`   | 解析失败，查看错误信息                 |
| `unparsed` | 未产出有效内容                         |

### 5.5 解析记录操作

| 操作           | 说明                                                       |
| -------------- | ---------------------------------------------------------- |
| 查看缩略图     | 点击图片可放大                                             |
| **编辑描述**   | AI 生成的描述可人工修正（推荐！AI 结果通常需微调）         |
| **编辑机器人ID** | 修改该记录归属的机器人                                    |
| **重新解析**   | 换了 LLM 模型或改了提示词后可重跑                          |
| **删除**       | 删除该条记录                                               |

### 5.6 机器人ID 筛选

规则与知识库一致：

- `全部机器人` — 显示所有记录
- `通用（空）` — 只显示未指定机器人的记录
- 具体ID — 显示该机器人 + 通用（空）的记录

### 5.7 全局搜索

搜索框在流程树页做**全局检索**，不受科室 / 平台筛选限制。

**检索范围**（任一命中即返回该流程树库）：

| 检索字段              | 说明                                            |
| --------------------- | ----------------------------------------------- |
| **流程树库描述**      | 库自身的 `description`                        |
| **记录文件名**        | 上传的图片 / PDF 文件名                         |
| **流程描述**          | AI 解析出的自然语言描述（**最常用**）           |
| **结构信息**          | 解析产出的页面元信息 JSON                       |
| **机器人ID**          | 记录归属的机器人                                |

**行为**：

1. 后端按上述字段模糊匹配，返回所有命中的流程树库
2. 前端自动选中第一个命中的库
3. 右侧只显示命中关键词的记录
4. 若关键词命中的是**流程树库自身的描述**，则保留该库下**全部记录**（避免筛成空列表）
5. 搜索**大小写不敏感**，中英文混排也能命中

> **典型用法**：想找"访客问地址时怎么应对"，直接搜 `地址` 或 `门店`，就能定位到相关流程描述所在的库与记录。

### 5.8 解析质量说明

系统会自动判断当前 LLM 是否支持读图：

| LLM 类型                                        | 处理方式                     |
| ----------------------------------------------- | ---------------------------- |
| **支持视觉**（`qwen-vl-max` / `gpt-4o` 等） | 直接读图 + OCR 文字辅助      |
| **不支持视觉**（`deepseek-v4-flash` 等）      | OCR 提取文字 + 纯文本理解    |

两种方式都能出结果，视觉模型对图形结构理解更精确。

> **输出预期**：60~180 字（最多 250 字）的一小段自然语言，只讲核心分支逻辑，不会完整还原整张图。城市名 / 门店名 / 姓名 / 手机号会自动脱敏。

---

## 6. 账户权限管理

> **仅 admin 可见**。点顶部 `账户权限` 或右上角 👤 图标进入。

### 6.1 左侧账户列表

卡片展示：用户名 · 权限等级标签 · 启用状态 · 更新时间。右上角 `+ 新建账号`。

### 6.2 新建 / 编辑账号

| 字段           | 说明                                                                |
| -------------- | ------------------------------------------------------------------- |
| **用户名**     | 新建时必填，编辑时不可改                                            |
| **新密码**     | 至少 6 位；编辑时**留空表示不修改**                                 |
| **权限等级**   | `高级权限`（admin，看全部） / `普通权限`（受管理科室限制）      |
| **账号状态**   | 启用 / 禁用（禁用后无法登录）                                       |
| **管理科室**   | **仅普通权限显示**；按住 `Ctrl` / `⌘` 多选                      |
| **页面权限**   | 三个模块 × 三种操作的勾选矩阵                                       |

### 6.3 页面权限矩阵

| 页面           | 查看                    | 编辑                    | 删除                      |
| -------------- | ----------------------- | ----------------------- | ------------------------- |
| **提示词管理** | `can_prompt_view`     | `can_prompt_edit`     | `can_prompt_delete`     |
| **知识库管理** | `can_knowledge_view`  | `can_knowledge_edit`  | `can_knowledge_delete`  |
| **流程树管理** | `can_flow_view`       | `can_flow_edit`       | `can_flow_delete`       |

- **查看** — 能否看到该模块入口与列表
- **编辑** — 能否新建 / 修改
- **删除** — 能否删除

### 6.4 管理科室（普通用户核心限制）

| 场景               | 行为                                                                |
| ------------------ | ------------------------------------------------------------------- |
| **列表查询**       | 提示词 / 知识库 / 流程树列表**自动过滤**，只返回管理科室内的数据    |
| **新建 / 修改**    | 目标科室不在管理范围 → 返回 `403 该科室不在你的管理范围内`        |
| **知识库导入**     | 越权科室的行被自动跳过（计入 `skipped`）                          |
| **顶部科室下拉**   | 只显示管理科室；`全部科室` 表示"我权限内的全部"                   |

> **⚠️ 重要**：普通用户如果**不选任何管理科室**，登录后看不到任何列表（只有空的管理后台入口）。顶部会显示 `⚠ 未配置管理科室` 提醒。

### 6.5 典型账号配置示例

| 角色           | 权限等级 | 管理科室            | 提示词      | 知识库          | 流程树      |
| -------------- | -------- | ------------------- | ----------- | --------------- | ----------- |
| **超级管理员** | 高级     | —（不受限）         | 查看+编辑+删除 | 查看+编辑+删除 | 查看+编辑+删除 |
| **植发运营**   | 普通     | 植发科              | 查看+编辑   | 查看+编辑+删除  | 查看+编辑   |
| **口腔运营**   | 普通     | 口腔科              | 查看+编辑   | 查看+编辑+删除  | 查看+编辑   |
| **多科室主管** | 普通     | 植发科+口腔科+皮肤科 | 查看+编辑   | 查看+编辑+删除  | 查看+编辑   |
| **只读审计**   | 普通     | 全部勾选            | 仅查看      | 仅查看          | 仅查看      |

### 6.6 修改自己的密码

任何用户都可以点右上角 🔒 图标 → 输入旧密码 + 新密码 → 保存。

---

## 7. LLM 配置

> 点右上角 ⚙️ 图标进入。用于配置流程树解析与 Agent 对话使用的大模型。

### 7.1 版本管理

支持维护**多个** OpenAI 兼容 API 配置，一键切换激活。

| 字段           | 说明                                                                   |
| -------------- | ---------------------------------------------------------------------- |
| **配置名称**   | 便于识别，如 `阿里-qwen-vl-max`                                      |
| **Base URL**   | 如 `https://dashscope.aliyuncs.com/compatible-mode/v1`               |
| **API Key**    | 密钥；查看时显示为 `****` 掩码，保存时含 `****` 则不覆盖原值        |
| **模型名称**   | 如 `qwen-vl-max` / `gpt-4o` / `deepseek-v4-flash`                  |

### 7.2 操作

| 操作         | 说明                                                        |
| ------------ | ----------------------------------------------------------- |
| `+ 新增`   | 添加一个新配置                                              |
| `激活`     | 设为当前生效配置（同一时刻只有一个生效）                     |
| `编辑`     | 修改配置；API Key 留 `****` 表示不改                      |
| `删除`     | 删除配置                                                    |

### 7.3 模型选择建议

| 用途                 | 推荐模型                                                          |
| -------------------- | ----------------------------------------------------------------- |
| **解析流程图（推荐）** | `qwen-vl-max` / `qwen-vl-plus` / `gpt-4o` / `glm-4v`      |
| 仅解析对话截图        | 任意兼容模型均可（系统自动 OCR 兜底）                             |
| Agent 文本对话        | 任意兼容模型                                                      |

### 7.4 切换后生效

1. 在页面点 `激活`
2. 访问 `http://<服务器IP>:8901/api/llm/refresh` 刷新 Agent 缓存（或重启 Agent 服务）
3. 系统会在下次解析时重新探测该模型的视觉能力

---

## 8. 常见操作场景速查

### 场景 1：给新科室 + 新平台上线一套配置

```
① 账户权限     → 给对应运营账号勾上该科室（管理科室）
② 提示词管理   → + 新建提示词
                 科室=甲状腺科, 平台=抖音, 场景=系统提示词
                 名称=thyroid_dy_system
                 写好内容 + 变量映射 → 保存
③ 提示词详情   → 点「编辑」→ 机器人配置看板 → + 新增条目
                 机器人ID=13240, 公司名=xx医院, 提示词版本=-1 → 保存
④ 知识库管理   → + 新建知识库（甲状腺科 / 抖音）
                 + 新增条目：分别录入 答疑 / 问诊 / 套联 / 违禁词
                 或直接用 📥 导入 批量上传 Excel
⑤ 流程树管理   → + 新建流程树库（甲状腺科 / 抖音）
                 + 上传解析 上传业务流程图 → 等 success → 人工微调描述
⑥ 等 30 秒     → 业务服务自动热更新，无需重启
```

### 场景 2：批量更新一批知识库内容

```
① 知识库管理 → 设好筛选（如 科室=植发科）
② 点 📤 导出 → 下载 Excel
③ Excel 里批量改内容 / 加行 / 调类型 / 调机器人ID
④ 点 📥 导入 → 上传改好的文件
⑤ 看提示：导入完成：新建 X，更新 Y，跳过 Z
```

### 场景 3：灰度验证新版提示词

```
① 提示词管理 → 编辑内容 → 填变更说明 → 保存（版本 v10 → v11）
② 详情页机器人配置看板 → 编辑各机器人：
   - 灰度机器人 A：提示词版本 = -1     （吃新版 v11）
   - 对照机器人 B：提示词版本 = 10     （锁定旧版 v10）
③ 观察两组效果对比
④ 验证通过 → 把 B 也改回 -1
⑤ 效果不佳 → 版本历史里回滚，或把 A 也锁到 10
```

### 场景 4：紧急回滚提示词

```
方式一（推荐，影响全部机器人）：
  提示词详情 → 版本历史 → 找到上一个正常版本 → 点「回滚」

方式二（只影响部分机器人）：
  机器人配置看板 → 编辑该机器人 → 提示词版本 填正常的版本号
```

### 场景 5：新运营入职开账号

```
① 账户权限 → + 新建账号
② 用户名 / 密码（≥6位）
③ 权限等级 = 普通权限
④ 管理科室 = 按住 Ctrl 多选其负责的科室
⑤ 页面权限：
   - 提示词管理：查看 ✅ 编辑 ✅ 删除 ❌
   - 知识库管理：查看 ✅ 编辑 ✅ 删除 ✅
   - 流程树管理：查看 ✅ 编辑 ✅ 删除 ❌
⑥ 保存 → 告知账号密码 → 提醒首次登录后改密码
```

### 场景 6：查找某句话术在哪个知识库里

```
① 知识库管理
② 搜索框输入话术片段（如"留个联系方式"）→ 回车
③ 系统全局搜索所有知识库内容
④ 自动选中第一个命中的库，右侧只显示命中的条目
⑤ 左侧列表可切换到其他命中的库
```

---

## 9. 常见问题排查

### Q1：登录后看不到任何列表

**原因**：普通用户未配置「管理科室」。

**排查**：顶部用户名旁是否显示 `⚠ 未配置管理科室`。

**解决**：admin 进入「账户权限」→ 编辑该账号 → 勾选管理科室 → 保存 → 该用户**重新登录**。

---

### Q2：保存提示词 / 知识库报 403

**提示**：`该科室不在你的管理范围内`

**原因**：普通用户尝试操作管理科室之外的数据。

**解决**：让 admin 补充该科室到你的「管理科室」，或换 admin 账号操作。

---

### Q3：机器人配置看板不显示

**原因**：查看态下，当前科室+平台没有任何机器人配置。

**解决**：点「编辑」进入编辑态，看板会显示空态行和 `+ 新增条目` 按钮。

---

### Q4：改了配置但业务侧没生效

**排查顺序**：

1. **等 30 秒** — 业务侧轮询周期
2. **确认提示词状态是"启用"** — 停用的取不到
3. **确认提示词名称规范** — 建议 `{科室}_{平台}_{场景}`
4. **确认机器人配置的提示词版本是 `-1`** — 若锁了旧版本号，新版本不会生效
5. **确认机器人配置状态是"启用"** — 停用的不会同步
6. 让开发查业务服务日志，应有 `[热更新] ...已同步` 字样

---

### Q5：页面显示异常 / 白屏 / 按钮点不动

**解决**：

1. **强刷** `Ctrl + F5`（Mac：`Cmd + Shift + R`）
2. 换无痕窗口打开
3. 若提示会话失效 → 重新登录（服务重启会清空所有会话）

---

### Q6：Excel 导入失败

| 报错                        | 原因与解决                                                 |
| --------------------------- | ---------------------------------------------------------- |
| `请上传 .xlsx 格式`       | 文件不是 `.xlsx` / `.xlsm`，另存为 xlsx                |
| `表头必须为 [...]`        | 表头必须**完全一致且顺序相同**：科室 / 平台 / 类型 / 机器人ID / 知识内容 |
| `无法解析 Excel`          | 文件损坏，重新导出一份作为模板                              |
| 导入后 `跳过 N` 数字很大   | 检查是否有空行、或科室不在你的管理范围                      |

> **最稳做法**：先点 `📤 导出` 拿到标准模板，在模板上编辑后再导入。

---

### Q7：流程图解析失败 / 结果不准

| 现象                | 排查                                                            |
| ------------------- | --------------------------------------------------------------- |
| 状态 `failed`     | 查看错误信息；常见是图片不清晰或 LLM 未配置                     |
| 描述内容不准        | 点「编辑描述」人工修正（推荐做法）                              |
| 想提高图形理解精度   | ⚙️ LLM 配置里激活视觉模型（`qwen-vl-max` / `gpt-4o`）       |
| 换了模型想重跑       | 点该记录的「重新解析」                                          |

---

### Q8：想让某条知识对所有机器人生效

**做法**：编辑该知识条目 → 机器人ID 选 `通用（空）` → 保存。

之后任何机器人检索时都会命中它。

---

### Q9：搜不到明明存在的知识

**排查**：

1. 确认在**知识库管理**页搜索（这里搜的是知识内容，全局生效）
2. 搜索是**模糊匹配**，试试更短的关键词片段
3. 若该知识属于你管理科室之外的库，普通用户搜不到
4. 检查类型 / 机器人筛选是否限制了结果范围

---

### Q10：账号密码忘了

**解决**：让 admin 进入「账户权限」→ 编辑该账号 → 填新密码 → 保存。

若 admin 密码也忘了，需要开发在服务器上重置数据库中的 admin 账号。

---

## 附录：字段速查表

> 下表为**系统默认值**。科室 / 平台 / 知识类型均可通过对应下拉框的 `⚙ 自定义...` 入口增删改，详见 [1.5](#15-自定义科室--平台--知识类型)。

### 科室枚举

| 代码             | 名称     | 代码            | 名称       |
| ---------------- | -------- | --------------- | ---------- |
| `hair`         | 植发科   | `thyroid`     | 甲状腺科   |
| `dentistry`    | 口腔科   | `psychiatry`  | 精神科     |
| `dermatology`  | 皮肤科   | `andrology`   | 男科       |
| `ophthalmology`| 眼科     | `gynaecology` | 妇科       |
| `pediatrics`   | 儿科     | `general`     | 通用       |
| `beauty`       | 医美     |                 |            |

### 平台枚举

| 代码         | 名称   | 代码        | 名称   |
| ------------ | ------ | ----------- | ------ |
| `xhs`      | 小红书 | `wechat`  | 微信   |
| `bd`       | 百度   | `general` | 通用   |
| `dy`       | 抖音   |             |        |
| `kuaishou` | 快手   |             |        |

### 场景枚举

| 代码              | 名称       | 说明                     |
| ----------------- | ---------- | ------------------------ |
| `system_prompt` | 系统提示词 | 主对话提示词             |
| `warmup`        | 暖场语     | 开场话术                 |
| `knowledge`     | 知识模板   | 知识注入模板             |
| `action_desc`   | 动作描述   | 回复意图描述             |
| `score`         | 评分       | 回复质量评分提示词       |
| `general`       | 通用       | 其他用途                 |

---

## 相关文档

| 文档                      | 面向对象      | 内容                                    |
| ------------------------- | ------------- | --------------------------------------- |
| `操作手册.md`            | 运营 / 业务方 | 本文件：逐页操作步骤 + 字段说明          |
| `README.md`             | 开发          | 架构 / 数据库 / API / SDK / 部署         |
| `AGENT_ARCHITECTURE.md` | 开发 / 架构   | Agent 架构判定 + 业界方案 + 演进路线     |
